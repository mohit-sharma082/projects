version: "3.8"
services:
  logstreamer:
    build: ./server
    image: docker-log-streamer:latest
    ports:
      - "3001:3001"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
